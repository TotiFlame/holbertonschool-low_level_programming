#include "main.h"

/**
* _strncpy - copies a string
* @dest: pointer
* @src: pointer
* Return: dest
*/

char *_strncpy(char *dest, char *src, int n)
{
	int cont;

	for (cont = 0; cont < n && dest[n] && src[n]; cont++)
	{
		
	}
	return (dest);
}
